import { Card } from "@/components/ui/Card";

export default function LoadingAdminProjects() {
  return (
    <div className="grid gap-4 p-6">
      <Card><p className="text-sm text-muted">Loading project operations...</p></Card>
    </div>
  );
}
